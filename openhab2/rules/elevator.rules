rule "Power Mode Options"
when
    Item PowerMode changed
then
    switch (PowerMode.state as DecimalType) {
        case 0 : PowerStatus.sendCommand(OPEN)
        case 1 : PowerStatus.sendCommand(CLOSED)
        case 2 : {
            //put auto code here or trigger another rule to execute auto code
        }
    }
end
